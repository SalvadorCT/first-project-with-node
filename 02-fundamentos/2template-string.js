const nombre = "jose";
const real = "maria";

const normal = nombre + " " + real;
const template = `${nombre} ${real}`;
// backticks `` > template string
// backticks `` > concatenar variables

// template string > permite saltos de linea

console.log(normal);

const html = `
    <h1>Hola</h1>
    <p>Mundo</p>
`;

console.log(html);