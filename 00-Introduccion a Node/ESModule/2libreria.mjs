//CommonJS
// sirve para indicaar al compilador que se tiene que ejecutar de arriba hacia abajo

//en los archivos de ESModule el 'use strict' no es necesario
//ya viene por defecto

export const sumar = (num1,num2) => {
    return num1 + num2;
}

export const restar = (num1,num2) => {
    return num1 - num2;
}
